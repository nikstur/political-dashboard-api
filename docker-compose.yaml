version: '3'
services:
  receiver:
    build: ./receiver
    depends_on:
      - db
    # restart: always
  db:
    image: mongo:4.2.6
    logging:
      driver: none
    # restart: always
  api:
    build: ./api
    environment:
      - DB_HOSTNAME=db
    depends_on:
      - receiver
      - db
    ports:
      - '8000:8000'
    # restart: always
