version: '3.8'
services:
  receiver:
    build: ./receiver
    init: true
    depends_on:
      - db
    # restart: always
  db:
    image: mongo:4.2.6
    init: true
    logging:
      driver: none
    # restart: always
  api:
    build: ./api
    init: true
    depends_on:
      - receiver
      - db
    ports:
      - '8000:8000'
    # restart: always
